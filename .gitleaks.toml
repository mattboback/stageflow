title = "StageFlow gitleaks config"

[allowlist]
description = "False positive: Podman libpod API prefix identifiers are not API keys."
paths = [
  '''services/orchestrator/internal/adapters/runtime/client\.go''',
]
regexes = [
  '''apiPrefix\\s*:=\\s*libpodV[0-9]+Prefix''',
]

