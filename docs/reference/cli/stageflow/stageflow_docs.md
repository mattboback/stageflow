## stageflow docs

Generate CLI documentation (Markdown)

```
stageflow docs
```

### Options

```
  -h, --help             help for docs
      --out-dir string   Output directory (relative to repo root when in a git repo) (default "docs/generated/cli")
```

### Options inherited from parent commands

```
      --api string       API base URL (default "http://localhost:8080")
      --api-key string   API key
      --format string    Output format: text, markdown, or json (default "text")
```

### SEE ALSO

* [stageflow](stageflow.md)	 - StageFlow CLI

