{
	"name": "stageflow-precommit-tooling",
	"private": true,
	"version": "0.0.0",
	"packageManager": "bun@1.3.8",
	"scripts": {
		"prepare": "husky",
		"precommit:web": "cd clients/web && bun run check && bun run test",
		"precommit:scanner": "cd services/scanner-runner && bun run type-check && bun run test",
		"precommit:contracts-report": "cd libs/contracts/report && ( [ -d node_modules ] || bun install --frozen-lockfile ) && bun run check",
		"precommit": "bun run precommit:web && bun run precommit:scanner && bun run precommit:contracts-report"
	},
	"devDependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0",
		"prettier": "^3.8.1",
		"prettier-plugin-svelte": "^3.5.1",
		"prettier-plugin-tailwindcss": "^0.7.2"
	}
}
